:root {
    --primary-color: #4CAF50; /* أخضر */
    --secondary-color: #f4f4f9; /* رمادي فاتح للخلفية */
    --button-bg: #fff;
    --text-color: #333;
    --card-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
}

body {
    font-family: 'Arial', sans-serif;
    background-color: var(--secondary-color);
    margin: 0;
    padding: 20px 10px;
    direction: rtl; 
    text-align: right;
    color: var(--text-color);
    line-height: 1.6;
}

.container {
    max-width: 650px;
    margin: 0 auto;
    background-color: #fff;
    padding: 20px;
    border-radius: 12px;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
}

header {
    text-align: center;
    margin-bottom: 25px;
    color: var(--primary-color);
}

header h1 {
    margin-top: 10px;
    font-size: 26px;
}

header i {
    font-size: 30px;
}

.card {
    background-color: #fff;
    padding: 20px;
    margin-bottom: 20px;
    border-radius: 10px;
    border: 1px solid #eee;
}

h2 {
    color: var(--primary-color);
    border-bottom: 1px solid #eee;
    padding-bottom: 10px;
    margin-top: 0;
    font-size: 20px;
}
h2 i {
    margin-left: 8px;
}

/* معلومات الموظف */
.user-info input {
    width: 100%;
    padding: 12px;
    margin-bottom: 15px;
    border: 1px solid #ddd;
    border-radius: 8px;
    box-sizing: border-box;
    font-size: 16px;
}

/* قائمة المشروبات */
.menu-items {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

.drink-btn {
    flex-basis: calc(33.33% - 7px); /* 3 أعمدة في الصف */
    padding: 15px 10px;
    border: 2px solid var(--primary-color);
    background-color: var(--button-bg);
    color: var(--primary-color);
    cursor: pointer;
    border-radius: 10px;
    font-size: 15px;
    font-weight: bold;
    transition: all 0.2s;
    text-align: center;
}
/* تنسيق الجوال (2 عمود) */
@media (max-width: 600px) {
    .drink-btn {
        flex-basis: calc(50% - 5px); 
    }
}

.drink-btn:hover {
    background-color: #e6ffe6;
}

.drink-btn.selected {
    background-color: var(--primary-color);
    color: #fff;
    border-color: #388e3c;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

/* التخصيص */
.current-order p {
    font-size: 17px;
    font-weight: bold;
    margin-bottom: 15px;
}
#selectedDrink {
    color: #007bff;
    font-weight: 900;
}

.addons {
    display: flex;
    gap: 10px;
    margin-bottom: 15px;
}

.addon-btn {
    flex: 1;
    padding: 12px;
    border: 1px solid #ccc;
    background-color: #f8f8f8;
    color: var(--text-color);
    cursor: pointer;
    border-radius: 8px;
    transition: all 0.2s;
}
.addon-btn i {
    margin-left: 5px;
}

.addon-btn.active {
    background-color: #ffc107; /* أصفر */
    color: #333;
    border-color: #e0a800;
}

textarea {
    width: 100%;
    height: 70px;
    padding: 10px;
    border: 1px solid #ddd;
    border-radius: 8px;
    resize: vertical;
    box-sizing: border-box;
    font-size: 14px;
}

/* زر الإرسال */
.submit-btn {
    width: 100%;
    padding: 18px;
    background-color: var(--primary-color);
    color: white;
    border: none;
    border-radius: 10px;
    font-size: 22px;
    font-weight: bold;
    cursor: pointer;
    margin-top: 25px;
    transition: background-color 0.2s;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.submit-btn:hover {
    background-color: #388e3c;
}

/* رسالة التأكيد */
.hidden {
    display: none;
}
.success-message {
    padding: 15px;
    margin-top: 20px;
    border-radius: 8px;
    text-align: center;
    font-weight: bold;
    background-color: #d4edda;
    color: #155724;
    border: 1px solid #c3e6cb;
}
